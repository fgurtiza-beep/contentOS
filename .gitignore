/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env*.local

# Source context material (kept out of the app build)
/*.pdf
/*.zip
.vercel

# Binary context PDFs (kept as reference in repo root only, gitignored there too)
docs/context/*.pdf
